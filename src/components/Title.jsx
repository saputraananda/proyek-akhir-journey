function Title({ title }) {
    return (
        <h2 className="text-2xl font-extrabold mt-6 bg-gradient-to-r from-blue-400 via-cyan-500 to-green-400 bg-clip-text text-transparent">
            {title}
        </h2>
    )
}

export default Title
